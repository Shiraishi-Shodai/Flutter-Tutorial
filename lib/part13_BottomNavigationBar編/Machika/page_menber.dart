import 'package:flutter/material.dart';

class Page_Menber extends StatelessWidget {
  const Page_Menber({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.amber,
      child: Text('会員画面です')
    );
  }
}