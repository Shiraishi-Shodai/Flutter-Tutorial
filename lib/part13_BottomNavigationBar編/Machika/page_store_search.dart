import 'package:flutter/material.dart';

class Store_Search extends StatelessWidget {
  const Store_Search({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
      child: Text('お店検索画面です')
    );
  }
}