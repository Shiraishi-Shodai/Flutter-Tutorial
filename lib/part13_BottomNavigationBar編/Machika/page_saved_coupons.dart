import 'package:flutter/material.dart';

class Page_Saved_Coupons extends StatelessWidget {
  const Page_Saved_Coupons({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.amber,
      child: Text('保存済みクーポン画面です')
    );
  }
}